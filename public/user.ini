; =============================================================================
; KTNB App — PHP settings cho shared hosting (cPanel / LiteSpeed)
; Cách dùng: copy vào thư mục public/ (cùng cấp index.php)
; =============================================================================

upload_max_filesize = 512M
post_max_size = 512M
max_execution_time = 600
memory_limit = 1024M
